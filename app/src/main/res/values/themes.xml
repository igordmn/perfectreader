<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.Toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="titleTextAppearance">@style/AppTheme.Toolbar.Title.Appearance</item>
        <item name="subtitleTextAppearance">@style/AppTheme.Toolbar.Subtitle.Appearance</item>
    </style>

    <style name="AppTheme.Toolbar.Title.Appearance" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/text_primary_light</item>
    </style>

    <style name="AppTheme.Toolbar.Subtitle.Appearance" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/text_primary_light</item>
    </style>

</resources>